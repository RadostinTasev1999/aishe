{
  "type": "module",
  "version": "1.0.0",
  "description": "Entrypoint for the AIshe CLI TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "build": "npm run build:link && tsc",
    "build:link": "npm link aishe-client",
    "start": "node dist/main.js"
  },
  "types": "dist/main.d.ts",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
